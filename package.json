{
  "name": "synthetic-user-testing",
  "version": "1.0.0",
  "description": "Synthetic UX research pipeline — pipeline + API server",
  "private": true,
  "scripts": {
    "build": "cd ui && npm install --legacy-peer-deps && npm run build",
    "start": "node src/server/index.js",
    "parse": "node src/pipeline/parseIntake.js",
    "plan": "NODE_EXTRA_CA_CERTS=certs.pem node src/pipeline/generatePlan.js",
    "run-study": "NODE_EXTRA_CA_CERTS=certs.pem node src/pipeline/evaluator.js",
    "compare": "NODE_EXTRA_CA_CERTS=certs.pem node src/pipeline/compare.js",
    "compare-plan": "NODE_EXTRA_CA_CERTS=certs.pem node src/pipeline/comparePlan.js",
    "certs": "security find-certificate -a -p /Library/Keychains/System.keychain > certs.pem && security find-certificate -a -p ~/Library/Keychains/login.keychain-db >> certs.pem && echo 'certs.pem updated'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.96.0",
    "@sparticuz/chromium": "^131.0.1",
    "@supabase/supabase-js": "^2.106.0",
    "cors": "^2.8.5",
    "docx": "^9.7.1",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "mammoth": "^1.8.0",
    "multer": "^2.1.1",
    "openai": "^6.37.0",
    "playwright-core": "^1.49.0",
    "sharp": "^0.34.5"
  }
}
